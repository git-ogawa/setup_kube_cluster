apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
networking:
  podSubnet: "{{ pod_network_cidr }}"
clusterName: {{ cluster_name }}
{% if ha_cluster.enabled | bool %}
controlPlaneEndpoint: {{ load_balancer_dns }}:{{ load_balancer_port }}
{% endif %}
