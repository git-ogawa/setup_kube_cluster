---
- name: Install python3.9 and pip
  dnf:
    name:
      - python39
      - python39-pip
    state: present
  become: true

- name: Install python dependencies
  pip:
    name:
      - kubernetes >= 12.0.0
      - pyyaml >= 3.11
      - jsonpatch
    state: present
    extra_args: --user
    executable: pip3.9
